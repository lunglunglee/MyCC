@model IEnumerable<MyApp.Models.C_代理>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.代理Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.代理Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.代理名稱 }) |
            @Html.ActionLink("Details", "Details", new { id=item.代理名稱 }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.代理名稱 })
        </td>
    </tr>
}

</table>
